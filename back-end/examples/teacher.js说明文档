####################################################################################################

teacher.js 里面含有teacher这个类

teacher类的属性：
  name 教师名称 类型string
  id 教师id 类型int
  avatarsUrl 头像的URL 类型string
  teacherType 教师的类型: 比如(班主任;AP老师;班主任) 类型string[]
  classesId 教的课程的id 类型int[]

teacher类里面的方法
调用函数可以参考同目录下的 teacher_examples.js

构造函数： 
  函数：constructor(name, id, avatarsUrl, teacherType, classesId) 返回值 void。
  调用实例：const teacherObject1 = new teacher("murphy", 0, "cn.bing.com", ["ap"], [0, 1, 2]);

  函数：static parseTeacherByString(jsonString) 返回值为teacher类。参数jsonString 类型是string。
  调用实例：const jsonString = `{ "name": "murphy", "id": 0, "avatarsUrl": "cn.bing.com", "teacherType": ["ap"], "classesId": [0, 1, 2]}`;
           const teacherObject2 = teacher.parseTeacherByString(jsonString);

  函数：static parseTeacherByJson(JSON) 返回值是 teacher类。参数JSON 类型是JSONObject。
  调用实例：const Json = { "name": "murphy", "id": 0, "avatarsUrl": "cn.bing.com", "teacherType": ["ap"], "classesId": [0, 1, 2]}
           const teacherObject3 = teacher.parseTeacherByJson(Json);

其他的函数或者方法：

static async getTeacherLists()异步函数，返回值是一个teacher类的数组。
  作用：读取数据库中全部的老师数据。

static async getTeacherById(teacherId)异步函数，返回值是一个teacher类。参数teacherId 类型是int
  作用：根据teacher这个类的属性中的id值来查找服务器中对应的老师。找不到返回null。

static async getTeacherByName(teacherName)异步函数，返回值是一个teacher类。参数teacherName 类型是string
  作用：根据teacher这个类的属性中的name值来查服务器中找对应的老师。找不到返回null。

static async getTeacherByType(typeName)异步函数，返回值是一个teacher类的数组。
  参数typeName 类型是string; 并且值仅为("ap", "ALevel", "SinoCanada", "president", "administrator", "HeadTeacher", "RegularTeacher", "CollegeCounselor", "other")其中一个
                            分别对应：ap老师，alevel老师，中加高中老师，校长，管理层老师，班主任，中方课程老师，升学指导老师，其他。
  作用：根据teacher这个类的属性中的teacherType数组和指定的职位(typeName)来筛选符合条件的老师。

static async getSearchSuggestion(keyWord)异步函数，返回值是一个string类的数组
  参数：keyWord 类型值是string
  作用：根据用户所收搜的keyWord来返回提示信息。比如：用户输入"张"，会返回所有在服务器中名字含有张老师的名字。找不到返回空数组。

async addTeacherToServer()异步函数，返回值是一个boolean。
  作用：添加一个新的老师，返回true和false来判断是否添加成功。

static async removeTeacherById(teacherId)异步函数，返回值是一个boolean。
  作用：根据teacher这个类的属性中的id查找老师并且删除，返回true和false来判断是否删除成功。

static async removeTeacherByName(teacherName)异步函数，返回值是一个boolean。
  作用：根据teacher这个类的属性中的name查找老师并且删除，返回true和false来判断是否删除成功。

async updateTeacherById(teacherId)异步函数，返回值是一个boolean。
  作用：根据teacher这个类的属性中的id查找老师并且更新信息，返回true和false来判断是否更新成功。

async updateTeacherByName(teacherName)异步函数，返回值是一个boolean。
  作用：根据teacher这个类的属性中的name查找老师并且更新信息，返回true和false来判断是否更新成功。

toJson()返回值是一个JSONObject
  作用：
